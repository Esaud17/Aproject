//object to declaration aplications
(function (Scope)
{
    function APP()
    {   //contructor del app
        this.initialize();
    };
    APP.prototype.initialize = function (){
        //initize aplication
    };
    //Block to Attributes
    var ObjectSData =  APP.prototype;

    //Block to validations
    APP.prototype.iswhitespace = function (value){
        if(value==null||value.length==0||/^\s+$/.test(value))
        {return false;}return true;
    };
    APP.prototype.isnumber= function (value){
        if(value==null||value.length==0||!/^([0-9])*$/.test(value))
        {return false;}return true;
    };
    APP.prototype.isalfanumeric= function (value) {
        if (value==null||value.length==0||!/^([a-zA-Z0-9_])+$/.test(value))
        {return false;}return true;
    };
    APP.prototype.isstring= function (value) {
        if (value==null||value.length==0||!/^([a-zA-Z_])+$/.test(value))
        {return false;}return true;
    };
    APP.prototype.isinteger= function (value) {
        if (value==null||value.length==0||!/^(?:\+|-)?\d+$/.test(value))
        {return false;}return true;
    };
    APP.prototype.isdouble= function (value) {
        if (value==null||value.length==0||/^(?:\+|-)?\d+$/.test(value))
        {return false;}return true;
    };
    //buid objects models to aplication dinamics files 
    APP.prototype.ObjectNewPlugin= function (Name,Ubication,Position,Logo, Description) {
        return {"Name":Name,"Ubication":Ubication,"Logo":Logo,"Position":Order,"Description":Description,"Visible":true};
    };
    APP.prototype.ObjectNewFolder= function (Name,Ubucation,Type,Position) {
        return {"Name":Name,"Ubication":Ubucation,"Position":Position,"Type":Type,"Visible":true};
    };
    APP.prototype.ObjectNewFile= function (Name,Ubication,Position,Preview) {
        return{"Name":Name,"Ubication":Ubication,"Postion":Position,"Preview":Preview};
    };
    APP.prototype.ObjectNewExep= function (Name,Format,Type) {
        return {"Name":Name,"Format":Format,"Type":Type};
    };
    APP.prototype.ObjectNewConfiguration= function (BackGround,Folder,Library) {
        return {"BackGround":BackGround,"Folders":Folder,"Library":Libray};
    };
    //Block to definitions this app selctions options
    
    Scope.APP= APP;
}(window));
//this object is wordl from control objects to real time app
(function(Scope )
{
  function WorldObjects()
    {   //contructor del app
        this.initialize();
    };

    var ObjectsArray =  WorldObjects.prototype;
    var Objectresponse =  WorldObjects.prototype;

    WorldObjects.prototype.initialize = function (){
        //initize aplication
    };

    WorldObjects.prototype.LoadJson = function(listener,data,functionaction,resul)
    {
        $.post(listener,data,functionaction,resul);

    }
 Scope.WorldObjects= WorldObjects;
}(window));